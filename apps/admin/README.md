# Admin App (MVP)

Static MVP client for admin flows:
- admin login
- view current session user
- view markets

## Run locally
Serve this folder with any static server. API defaults to `http://127.0.0.1:8787`.
Set `localStorage.wg_api` to change API URL.
